{##
	@description
	Renders a gallery and bootstraps the corresponding JavaScript API.

	@param {Enumeration} type
		default
		xxl
		hero
	@param {Enumeration} paginationMode
		graphical - displays a simple graphical page indicator
		none - displays no page indicator
	@param {String} class - Adds a custom class to the gallery root element
#}

{% macro m001_gallery(
		type='',
		paginationMode='graphical',
		class='') %}
    {% set content = 'default' %}
    {% set moduleClass = 'm001-gallery' %}
	<div class="{{ moduleClass }} {{ type }} {{ class }}">
        {{ e005_button(size='l', classes=moduleClass + '-button') }}
        {{ content }}
        {{ m001_gallery_page() }}
	</div>
{%  endmacro %}

{% macro m001_gallery_page(
        type='',
        class='') %}
    <div class="m001-gallery-page {{ type }} {{ class }}"></div>
{%  endmacro %}
